<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.mark.app.mkpay">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".strip.StripPayActivity"
            android:theme="@style/StripTheme.AppTheme"/>

        <meta-data
            android:name="MKPAY_METADATA_KEY_STRIP"
            android:value="${MKPAY_KEY_STRIP}" />

    </application>

</manifest>
